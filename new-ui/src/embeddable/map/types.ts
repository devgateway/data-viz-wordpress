export interface ***REMOVED*** {
    children: React.ReactNode,
    data?: any,
    measures: string,
    ***REMOVED***: any,
    ***REMOVED***: boolean,
    mapType: string,
    ***REMOVED***: string,
}